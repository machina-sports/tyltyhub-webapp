apiVersion: v1
kind: Service
metadata:
  name: machina-workspace-entain-bwin-app
  namespace: machina-workspace
spec:
  clusterIP: 
  internalTrafficPolicy: Cluster
  ipFamilies:
  - IPv4
  ipFamilyPolicy: SingleStack
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: machina-workspace-entain-bwin-app
  sessionAffinity: None
  type: ClusterIP 